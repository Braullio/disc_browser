<br><center><% text = ((@profile.count > 1) ? 'perfils' : 'profile') %>
<%= "Esse teste DISC apresenta #{@profile.count} #{text}: " %>
<% @profile.each.with_index do |p,index|%>
<%= @profile.count.equal?("#{index}".to_i+1) ? "#{p}" : "#{p} e " %>
<% end %></center>
<br>
<div class='row justify-content-around'>
    <% @profile.each do |profile|%>
    <%= raw t("disc_html.#{profile}") %>
    <% end %>
</div>
<center>
    <%=link_to 'Pagina Inicial', root_path , class: "btn text-white bg-dark"%>
</center>