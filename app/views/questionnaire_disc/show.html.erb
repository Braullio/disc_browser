<br>
<center>
    <% text = ((@profile.size > 1) ? 'perfis' : 'perfil') %>
    <%= "Esse teste DISC apresenta #{@profile.size} #{text}: " %>
    <% @profile.each.with_index do |p,index|%>
    <%= @profile.size.equal?("#{index}".to_i+1) ? "#{p}" : "#{p} e " %>
    <% end %>
</center>

<br>

<div class='row justify-content-around'>
    <% @profile.each do |profile|%>
    <%= raw t("disc_html.#{profile}") %>
    <% end %>
</div>

<center>
    <%=link_to 'Pagina Inicial', root_path , class: "btn text-white bg-dark"%>
</center>